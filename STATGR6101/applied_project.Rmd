---
title: "Applied project: UEFA Champions League"
output: html_notebook
---
Packages 

```{r}
library(zoo)
```
Pre-processing of the data. 

```{r}
ucldf = read.csv("/Users/clairehe/Desktop/UEFA_CL_Player_stats.csv", header=T)
head(ucldf)

data <- as.matrix(is.na(ucldf))
for (cols in 1:length(ncol)){
  data[,cols]<- as.integer(data[,cols])
}
heatmap(data,Colv = NA, Rowv = NA, scale="column")
```

Missing data assessment
```{r}
ucldf <- ucldf[ucldf$X !="NA",]
```

```{r}
colSums(is.na(ucldf))
```

```{r}
ucldf
```

# Descriptive statistics

```{r}
summary(panel)
```

Recode categorical variables 

```{r}
panel$POINT_TYPE <- as.factor(panel$POINT_TYPE)
panel$STATUS <- as.factor(panel$STATUS)
```


```{r}
fit<- lm(tlimth ~ ., data=tli)
summary(fit)
```





```{r}
file <- "/Users/clairehe/Downloads/IST_corrected.txt"

ist_data <- read.table(file, sep=",", header=TRUE)
```

```{r}
head(ist_data)
```
```{r}
coln <- c()
df_ist <- ist_data[, coln]

```





